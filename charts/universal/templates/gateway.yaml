{{- if .Values.gateway.enabled -}}
# Gateway API HTTPRoute - placeholder for future implementation
# Requires Gateway API CRDs to be installed in the cluster
#
# Example usage in values.yaml:
# gateway:
#   enabled: true
#   parentRefs:
#     - name: my-gateway
#       namespace: gateway-system
#   hostnames:
#     - app.example.com
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: my-service
#           port: 80
#
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: {{ .Release.Name }}
#   namespace: {{ .Release.Namespace }}
#   labels:
#     universal/app-name: {{ .Release.Name }}
# spec:
#   parentRefs: []
#   hostnames: []
#   rules: []
{{- end -}}
